export const EST_REFRESH_PERIOD = 17; // ms // estimated refresh time of requestFrame
export const GRID_LENGTH = 320;
export const LOOKAHEAD_TIME = 60; //ms
export const INIT_TEMPO = 0.5; // s || Time before goind to the first BAR (while currentNote is null)
